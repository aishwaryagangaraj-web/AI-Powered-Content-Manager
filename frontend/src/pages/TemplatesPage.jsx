import { ArrowRight, Bookmark, Heart, Plus, Search, Sparkles } from 'lucide-react'
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import PageHeader from '../components/PageHeader'
import { useSaaSStore } from '../store/saasStore'
import { useToastStore } from '../store/uiStore'

const categories=['All','Marketing','Email','SEO','Social','Sales','Career']
export default function TemplatesPage(){const {templates,toggleTemplate,addTemplate,saveDraft}=useSaaSStore();const [category,setCategory]=useState('All');const [search,setSearch]=useState('');const [open,setOpen]=useState(false);const [form,setForm]=useState({name:'',category:'Marketing',description:'',prompt:''});const navigate=useNavigate();const addToast=useToastStore(s=>s.addToast);const visible=templates.filter(t=>(category==='All'||t.category===category)&&(`${t.name} ${t.description}`.toLowerCase().includes(search.toLowerCase())));const use=t=>{saveDraft({title:t.name,content:`<h2>${t.name}</h2><p>${t.prompt}</p>`});navigate('/app/workspace');addToast('Template opened in AI Workspace.')};const submit=e=>{e.preventDefault();addTemplate(form);setOpen(false);addToast('Custom template saved.')};return <div className="space-y-6"><PageHeader eyebrow="AI Marketplace" title="Start with a proven prompt" description="Discover expert workflows, save favorites, and turn your best prompts into reusable team templates." actions={<button onClick={()=>setOpen(true)} className="btn-primary"><Plus size={16}/>Create template</button>}/><div className="card flex flex-col gap-3 p-3 lg:flex-row lg:items-center"><label className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} className="input pl-9" placeholder="Search the template marketplace…"/></label><div className="scrollbar flex gap-1 overflow-x-auto">{categories.map(x=><button key={x} onClick={()=>setCategory(x)} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs font-semibold ${category===x?'bg-slate-900 text-white dark:bg-white dark:text-slate-900':'hover:bg-slate-100 dark:hover:bg-slate-800'}`}>{x}</button>)}</div></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map((t,i)=><article key={t.id} className="card group flex min-h-60 flex-col p-5 transition duration-300 hover:-translate-y-1 hover:shadow-soft"><div className="flex items-start justify-between"><span className={`grid h-11 w-11 place-items-center rounded-xl ${['bg-brand-50 text-brand-600','bg-violet-50 text-violet-600','bg-amber-50 text-amber-600'][i%3]}`}><Sparkles size={20}/></span><button onClick={()=>toggleTemplate(t.id)} className={`rounded-lg p-2 ${t.favorite?'text-rose-500':'text-slate-300 hover:text-rose-500'}`}><Heart size={18} fill={t.favorite?'currentColor':'none'}/></button></div><div className="mt-5 flex items-center gap-2"><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-slate-500 dark:bg-slate-800">{t.category}</span>{t.custom&&<span className="text-[10px] font-semibold text-brand-600">Custom</span>}</div><h3 className="mt-3 font-semibold">{t.name}</h3><p className="mt-2 flex-1 text-sm leading-6 text-slate-500">{t.description}</p><button onClick={()=>use(t)} className="mt-5 flex items-center justify-between border-t pt-4 text-sm font-semibold text-brand-600">Use template <ArrowRight size={16} className="transition group-hover:translate-x-1"/></button></article>)}</section>{!visible.length&&<div className="card grid min-h-64 place-items-center border-dashed text-center"><div><Bookmark className="mx-auto text-slate-300"/><h3 className="mt-3 font-semibold">No templates found</h3><p className="mt-1 text-sm text-slate-500">Try a different category or search term.</p></div></div>}{open&&<div className="modal-backdrop"><form onSubmit={submit} className="modal-panel"><h3 className="text-lg font-semibold">Create custom template</h3><p className="mt-1 text-sm text-slate-500">Save a repeatable workflow for your team.</p><label className="mt-5 block"><span className="label">Template name</span><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="input"/></label><div className="mt-4 grid grid-cols-2 gap-3"><label><span className="label">Category</span><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="input">{categories.slice(1).map(x=><option key={x}>{x}</option>)}</select></label><label><span className="label">Description</span><input required value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="input"/></label></div><label className="mt-4 block"><span className="label">Prompt</span><textarea required value={form.prompt} onChange={e=>setForm({...form,prompt:e.target.value})} className="input min-h-28" placeholder="Write instructions and use [variables] for reusable fields."/></label><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="btn-secondary">Cancel</button><button className="btn-primary">Save template</button></div></form></div>}</div>}
